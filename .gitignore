# Python bytecode
__pycache__/
*.py[cod]
*$py.class

myplot.tex

# cache files
*.hsaco
*.source

# profiling / rocprofv3 / ATT / counter scratch (rocprof + ATT + counter tooling, both wave routes)
tmp/
att_output/
*_rocprof_trace/
rocprof_*/
.rocprofv3/
counter_tmp/
att_matmul.json
counters.yaml
gpucore.*

# per-kernel Triton cache (TRITON_CACHE_DIR)
cache/
# Private counter-collection work — kept locally, deliberately not tracked
kernels/attention/att_stall_counters.yaml
kernels/attention/private_counter_collection_recipe.md
# ATT config variants used only by the above (all-SE, and SIMD0-only). The
# tracked att_attn_se0.json is the one the documented recipes use.
kernels/attention/att_attn.json
kernels/attention/att_attn_simd0.json
# The optimization notebook and the two figures only it used: kept locally,
# deliberately not published
kernels/attention/note.md
kernels/attention/images/eff_tflops_curve.svg
kernels/attention/images/eff_tflops_scheduling.svg
